# 统一下拉选择组件 ui/Select Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建主题化 `ui/Select`（popover 式）替换 6 个原生 `<select>`，下拉观感与应用一致。

**Architecture:** `ui/select.tsx` 套用现有 `ui/popover`（对齐 ModelSwitcher）：受控触发器 + 主题化选项列表。迁移 SkillEditor(1) + AutomationForm(5)。纯函数 `selectedLabel` 供显示 + 单测。零后端。

**Tech Stack:** React + Tailwind(token) + ui/popover；vitest；门禁 tsc + vitest + build。

## Global Constraints

- 工作目录 `desktop/`。桌面渲染层，零后端。
- 复用 `ui/popover`（`Popover`/`PopoverTrigger asChild`/`PopoverContent`），审美对齐 `ModelSwitcher`；全走 token（border/bg-surface/fg/accent），深浅色自适应。
- 各原生 select 的 `data-testid` 迁移后落到 `<Select>` 触发器;不改各表单业务逻辑/校验，只换控件。
- 组件层无 RTL：纯逻辑(`selectedLabel`)走 vitest；UI 走 typecheck + build + 眼验。门禁：`npm run typecheck` + `npm run test`(不回归) + `npm run build` 全绿。
- 无密钥面。提交前 `git diff --cached | grep -iE "api[_-]?key|secret|sk-|Bearer"`。
- commit trailer：`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01G49KyPFe5h2vqV4zGSueLN`。
- 分支：接在 `feat/skill-scope-move`（当前）。

## File Structure

- `desktop/src/renderer/components/ui/select.tsx` — 新建组件 + `selectedLabel`。
- `desktop/test/select.test.ts` — 新建 `selectedLabel` 单测。
- `desktop/src/renderer/components/SkillEditor.tsx` — 来源 select → `<Select>`。
- `desktop/src/renderer/components/AutomationForm.tsx` — 5 个 select → `<Select>`。

---

### Task 1: ui/Select 组件 + selectedLabel + 单测

**Files:**
- Create: `desktop/src/renderer/components/ui/select.tsx`
- Test: `desktop/test/select.test.ts`

**Interfaces:**
- Produces: default export `Select`（props 见下）；具名导出 `selectedLabel(options, value): string|null` 与 `interface SelectOption { value: string; label: string }` —— 供 Task 2/3 迁移使用。

- [ ] **Step 1: 写失败测试**

新建 `desktop/test/select.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { selectedLabel } from '../src/renderer/components/ui/select'

describe('selectedLabel', () => {
  const opts = [{ value: 'user', label: '用户' }, { value: 'project', label: '项目' }]
  it('命中返回对应 label', () => {
    expect(selectedLabel(opts, 'project')).toBe('项目')
  })
  it('未命中返回 null', () => {
    expect(selectedLabel(opts, 'nope')).toBeNull()
  })
  it('空 options 返回 null', () => {
    expect(selectedLabel([], 'user')).toBeNull()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/aa00945/Desktop/wraith/desktop && npx vitest run test/select.test.ts`
Expected: FAIL —— 模块 `../src/renderer/components/ui/select` 不存在。

- [ ] **Step 3: 写组件**

新建 `desktop/src/renderer/components/ui/select.tsx`：

```tsx
import { useState } from 'react'
import { Popover, PopoverTrigger, PopoverContent } from './popover'

export interface SelectOption { value: string; label: string }

interface SelectProps {
  value: string
  options: SelectOption[]
  onChange: (value: string) => void
  disabled?: boolean
  placeholder?: string
  testId?: string
  className?: string
  contentClassName?: string
}

/** 当前 value 对应 option 的 label;无匹配返 null。 */
export function selectedLabel(options: SelectOption[], value: string): string | null {
  const hit = options.find(o => o.value === value)
  return hit ? hit.label : null
}

export default function Select({
  value, options, onChange, disabled = false,
  placeholder = '请选择', testId, className = '', contentClassName = '',
}: SelectProps): JSX.Element {
  const [open, setOpen] = useState(false)
  const label = selectedLabel(options, value)
  return (
    <Popover open={open} onOpenChange={setOpen}>
      <PopoverTrigger asChild>
        <button
          type="button"
          data-testid={testId}
          disabled={disabled}
          className={
            'flex items-center gap-1.5 rounded-lg border border-border bg-surface px-2.5 py-1.5 text-xs text-fg ' +
            'hover:border-accent disabled:cursor-not-allowed disabled:opacity-50 ' + className
          }
        >
          <span className={'min-w-0 flex-1 truncate text-left ' + (label ? '' : 'text-fg-subtle')}>
            {label ?? placeholder}
          </span>
          <span className="shrink-0 text-fg-subtle">▾</span>
        </button>
      </PopoverTrigger>
      <PopoverContent className={'min-w-[8rem] ' + contentClassName}>
        {options.length === 0 && (
          <div className="px-2 py-1.5 text-xs text-fg-subtle">无可选项</div>
        )}
        {options.map(o => {
          const active = o.value === value
          return (
            <button
              key={o.value}
              type="button"
              role="option"
              data-value={o.value}
              onClick={() => { onChange(o.value); setOpen(false) }}
              className={
                'mb-0.5 flex w-full items-center gap-1.5 rounded-md px-2 py-1.5 text-left text-xs ' +
                (active ? 'bg-surface text-fg' : 'text-fg-muted hover:bg-surface/60')
              }
            >
              <span className="min-w-0 flex-1 truncate">{o.label}</span>
              {active && <span className="ml-auto shrink-0">✓</span>}
            </button>
          )
        })}
      </PopoverContent>
    </Popover>
  )
}
```

- [ ] **Step 4: 跑测试确认通过 + typecheck**

Run: `cd /Users/aa00945/Desktop/wraith/desktop && npx vitest run test/select.test.ts && npm run typecheck`
Expected: vitest 3 passed；typecheck exit 0。

- [ ] **Step 5: 提交**

```bash
cd /Users/aa00945/Desktop/wraith/desktop
git add src/renderer/components/ui/select.tsx test/select.test.ts
git diff --cached | grep -iE "api[_-]?key|secret|sk-|Bearer" || echo "no secret hits"
git commit -m "$(cat <<'EOF'
feat(desktop): ui/Select 主题化下拉组件(popover 式,对齐 ModelSwitcher)

受控 <Select value options onChange>,触发器+选项列表全走 token 主题化,替代
原生 select 无法主题化的系统弹层。selectedLabel 纯函数供显示+单测。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01G49KyPFe5h2vqV4zGSueLN
EOF
)"
```

---

### Task 2: 迁移 SkillEditor 来源 select

**Files:**
- Modify: `desktop/src/renderer/components/SkillEditor.tsx`

**Interfaces:**
- Consumes: `Select`（default）+ `SelectOption`（Task 1）。

- [ ] **Step 1: import + 替换 select**

在 `SkillEditor.tsx` 顶部 import 区加：

```tsx
import Select from './ui/select'
```

把来源那段（约 :62-68 的 `<label>…<select …>…</select></label>`）里的 `<select>…</select>` 替换为 `<Select>`：

```tsx
          <label className="flex flex-col gap-1">
            <span className="text-2xs text-fg-subtle">来源</span>
            <Select
              testId="skill-scope-select"
              value={form.scope}
              disabled={lockScope}
              onChange={v => set('scope', v as 'user' | 'project')}
              options={[
                { value: 'user', label: '用户(~/.wraith/skills)' },
                { value: 'project', label: '项目(<项目>/.wraith/skills)' },
              ]}
            />
          </label>
```

（保留外层 `<label>` 与"来源"文字;`inputCls` 不再用于此控件。）

- [ ] **Step 2: 门禁**

Run: `cd /Users/aa00945/Desktop/wraith/desktop && npm run typecheck && npm run test && npm run build`
Expected: typecheck 0；vitest 不回归；build 成功。

- [ ] **Step 3: 提交**

```bash
cd /Users/aa00945/Desktop/wraith/desktop
git add src/renderer/components/SkillEditor.tsx
git diff --cached | grep -iE "api[_-]?key|secret|sk-|Bearer" || echo "no secret hits"
git commit -m "$(cat <<'EOF'
feat(desktop): SkillEditor 来源下拉迁移到 ui/Select

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01G49KyPFe5h2vqV4zGSueLN
EOF
)"
```

---

### Task 3: 迁移 AutomationForm 的 5 个 select

**Files:**
- Modify: `desktop/src/renderer/components/AutomationForm.tsx`

**Interfaces:**
- Consumes: `Select`（Task 1）。

- [ ] **Step 1: import**

在 `AutomationForm.tsx` 顶部 import 区加 `import Select from './ui/select'`。

- [ ] **Step 2: 替换 5 个 select（保留各自 data-testid 到触发器；number 值转 String）**

(a) 项目（约 :164）:
```tsx
        <Select
          testId="automation-form-project"
          className="mt-1 w-full"
          value={projectPath}
          onChange={setProjectPath}
          options={projects.map(p => ({ value: p.path, label: p.name || p.path }))}
        />
```

(b) 调度类型（约 :173）:
```tsx
          <Select
            testId="automation-form-schedule-kind"
            className="mt-1"
            value={kind}
            onChange={v => setKind(v as AutomationSchedule['kind'])}
            options={[
              { value: 'interval', label: '每隔 N 分钟' },
              { value: 'daily', label: '每天' },
              { value: 'weekly', label: '每周' },
              { value: 'cron', label: 'cron 表达式' },
            ]}
          />
```

(c) 星期（约 :190，number ↔ String）:
```tsx
            <Select
              testId="automation-form-schedule-weekday"
              className="mt-1"
              value={String(weekday)}
              onChange={v => setWeekday(Number(v))}
              options={WEEKDAYS.map((w, i) => ({ value: String(i), label: w }))}
            />
```

(d) 审批默认（约 :244）:
```tsx
          <Select
            testId="automation-form-approval-default"
            value={approvalDefault}
            onChange={v => setApprovalDefault(v as ApprovalMode)}
            options={APPROVAL_MODES.map(m => ({ value: m, label: approvalModeLabel(m) }))}
          />
```

(e) 工具覆盖模式（约 :263，在 `toolOverrides.map` 内）:
```tsx
                <Select
                  testId={`automation-form-tool-override-mode-${idx}`}
                  value={row.mode}
                  onChange={v => updateToolOverride(row.id, 'mode', v)}
                  options={APPROVAL_MODES.map(m => ({ value: m, label: approvalModeLabel(m) }))}
                />
```

每处：删除原 `<select …>…{…map(<option>)}…</select>`，换成上面的 `<Select>`;外层 `<label>`/文字不动。`updateToolOverride(row.id, 'mode', v)` 的第三参原是 `e.target.value`（string），现直接传 `v`（string），类型一致。

- [ ] **Step 3: 门禁**

Run: `cd /Users/aa00945/Desktop/wraith/desktop && npm run typecheck && npm run test && npm run build`
Expected: typecheck 0；vitest 不回归；build 成功。

- [ ] **Step 4: 检查 e2e 引用（风险项）**

Run: `cd /Users/aa00945/Desktop/wraith/desktop && grep -rn "selectOption\|automation-form-schedule-kind\|automation-form-project\|automation-form-approval-default" e2e 2>/dev/null || echo "no e2e refs"`
- 若命中 `selectOption(...)` 驱动这些 testid：把该 e2e 步骤改为"点击触发器 → 点击对应 `[role=option][data-value=…]`"。若只是断言存在/可见，无需改。若无 e2e 目录或无命中，跳过。

- [ ] **Step 5: 提交**

```bash
cd /Users/aa00945/Desktop/wraith/desktop
git add src/renderer/components/AutomationForm.tsx
# 若 Step 4 改了 e2e,一并 add 对应文件
git diff --cached | grep -iE "api[_-]?key|secret|sk-|Bearer" || echo "no secret hits"
git commit -m "$(cat <<'EOF'
feat(desktop): AutomationForm 5 个下拉迁移到 ui/Select

项目/调度类型/星期/审批默认/工具覆盖模式,全换主题化 Select;星期 number 值
经 String()↔Number() 转换,各 data-testid 沿用。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01G49KyPFe5h2vqV4zGSueLN
EOF
)"
```

---

## 交付后（人工/主循环）

- 眼验（运行桌面 App）：SkillEditor 来源 + AutomationForm 5 处下拉均为主题化弹层（非系统样式）、选中 ✓、hover 高亮、深浅色可读；SkillEditor 来源仍受 lockScope（编辑态可选/锁态禁用）；AutomationForm 选择后表单值正确（尤其星期数字、切换调度类型显隐联动）。
- 与 `feat/skill-scope-move` 其它改动一起眼验通过 → FF-merge + 推送（推送前用户点头）。纯桌面，jar 不变。

## Self-Review

**1. Spec 覆盖：** ui/Select 组件(popover/触发器/选项/token) → Task 1。✓ selectedLabel + vitest → Task 1。✓ 迁移 SkillEditor → Task 2。✓ 迁移 AutomationForm 5 处(含 weekday number 转换、testid 保留) → Task 3。✓ e2e 风险 → Task 3 Step 4。✓ 测试策略(纯函数 vitest + UI typecheck/build/眼验) → 各任务。✓
**2. 占位符扫描：** 无 TBD；组件与 6 处迁移均给完整代码;Task 3 Step 4 是条件性 e2e 检查(命中才改),非占位。✓
**3. 类型/命名一致性：**
- `Select`(default) + `selectedLabel`/`SelectOption`(具名):Task 1 定义,Task 2/3 import 一致。✓
- props `value/options/onChange/disabled/testId/className`:Task 1 定义与 Task 2/3 调用一致。✓
- weekday:`value={String(weekday)}` + `onChange={v=>setWeekday(Number(v))}`,与既有 number 状态一致。✓
- `setKind(v as AutomationSchedule['kind'])`、`setApprovalDefault(v as ApprovalMode)`、`updateToolOverride(row.id,'mode',v)` 均 string→原类型,与既有 onChange 语义一致。✓
