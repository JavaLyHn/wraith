# ContextCurator Phase D — 真实小样本 A/B 报告

> 由 `scripts/curator-ab/`(driver 真跑 wraith app-server + analyze 汇总)自动生成。**所有数字来自真实 DeepSeek 会话**(模型/窗口见下),费用按 PricingTable 的 deepseek-v4-flash 种子价折算(USD/百万:cacheHit 0.0028 / cacheMiss 0.14 / output 0.28)。

**口径**:input/output/cached = 会话末 `context.state.get` 的内存累计(curator.stats,on/off 均记);compaction/回取 = 通知流统计;鬼打墙 = 相同 tool+args 调用≥2 次的额外次数;input/工具 = 累计 input÷工具调用数(归一化探索量)。

> ⚠ **先读这条**:真实 agent 会话非确定性,各会话工具调用次数差异大 → 累计 input/费用主要受探索深度影响,**逐任务费用差不能干净归因给 curator**(详见文末「头号诚实边界」)。干净的量化节省数字见 `phase-d-bench.md`(确定性 bench)。

**模型**:`DeepSeek-V4-Flash`,窗口 128000 token。

## 任务 t1-curator-pkg

| 配置 | 状态 | 工具调用 | 累计 input | **input/工具** | cached | cache 命中率 | 费用(USD) | 压缩 | 鬼打墙 |
|---|---|--:|--:|--:|--:|--:|--:|--:|--:|
| off(legacy 对照) | completed | 30 | 201501 | **6717** | 37376 | 18.5% | $0.02491 | 0 | 0 |
| on·默认阈值 | completed | 32 | 125992 | **3937** | 36608 | 29.1% | $0.01395 | 0 | 1 |
| on·压力阈值 | completed | 28 | 123510 | **4411** | 43776 | 35.4% | $0.01244 | 1 | 0 |

- 费用差(默认)= off($0.02491) − on-default($0.01395) = **$0.01096** ⚠工具次数 30 vs 32,差异含探索深度
- 费用差(压力)= off($0.02491) − on-stress($0.01244) = **$0.01247** ⚠工具次数 30 vs 28,差异含探索深度

## 任务 t2-im-gateways

| 配置 | 状态 | 工具调用 | 累计 input | **input/工具** | cached | cache 命中率 | 费用(USD) | 压缩 | 鬼打墙 |
|---|---|--:|--:|--:|--:|--:|--:|--:|--:|
| off(legacy 对照) | completed | 81 | 758625 | **9366** | 523264 | 69.0% | $0.03729 | 0 | 1 |
| on·默认阈值 | completed | 41 | 193266 | **4714** | 70144 | 36.3% | $0.01951 | 0 | 0 |
| on·压力阈值 | completed | 59 | 386526 | **6551** | 181504 | 47.0% | $0.03148 | 0 | 0 |

- 费用差(默认)= off($0.03729) − on-default($0.01951) = **$0.01778** ⚠工具次数 81 vs 41,差异含探索深度
- 费用差(压力)= off($0.03729) − on-stress($0.03148) = **$0.00581** ⚠工具次数 81 vs 59,差异含探索深度

## 任务 t3-e2e-testids

| 配置 | 状态 | 工具调用 | 累计 input | **input/工具** | cached | cache 命中率 | 费用(USD) | 压缩 | 鬼打墙 |
|---|---|--:|--:|--:|--:|--:|--:|--:|--:|
| off(legacy 对照) | completed | 74 | 854715 | **11550** | 581632 | 68.0% | $0.04525 | 0 | 2 |
| on·默认阈值 | completed | 36 | 531997 | **14778** | 191488 | 36.0% | $0.05196 | 0 | 0 |
| on·压力阈值 | completed | 61 | 981707 | **16094** | 667392 | 68.0% | $0.04984 | 11 | 0 |

- 费用差(默认)= off($0.04525) − on-default($0.05196) = **$-0.00671** ⚠工具次数 74 vs 36,差异含探索深度
- 费用差(压力)= off($0.04525) − on-stress($0.04984) = **$-0.00459** ⚠工具次数 74 vs 61,差异含探索深度

## ⚠ 头号诚实边界:真实 agent 会话非确定性,费用差不能干净归因给 curator

本批每格只 1 个真实会话,且 agent 探索是非确定性的——**各会话工具调用次数差异巨大**(off 组普遍多跑),而累计 input/费用主要由"跑了多少个工具回合"决定。因此:

- 上方各任务的「费用差」跨度从 +50% 到 -15%(t3 甚至 on 比 off 贵),**这个方差主要来自探索深度不同,不是 curator 的净效应**。
- 把这些逐任务费用差当作"curator 省了 X%"是**误导**;本报告不做此断言(诚实红线)。
- 仅供参考的合计费用差:默认 $0.02203、压力 $0.01369(同样受探索方差污染,不作为验收结论)。

## 真实 A/B 能诚实得出的结论

1. **机制在生产路径可用**:9/9 会话全部 `completed`,curator 开启不破坏真实 DeepSeek agent 运行。
2. **压力下真实触发压缩**:on-stress 观察到真实压缩(如 t3 达 11 次),tier 通知流按预期发出;off/legacy 全程 0 压缩。
3. **归一化后有减载迹象**(非结论,受方差限):部分任务 on 的「input/工具」低于 off(如 t1、t2 默认),提示每次 LLM 调用携带的上下文更小;但 t3 反向,样本不足以定论。
4. **可回取率**:on 压缩把工具全量落盘可回取(见回取列),off/legacy 直接截断丢弃——这是定性能力差,不随方差变。

## 干净的量化证据在确定性 bench(§9.3)

真实小样本受 agent 非确定性限制,无法给出干净的 token/费用节省数字。**受控的量化结论以 `phase-d-bench.md` 为准**(固定脚本、固定探索、curator on/off 同输入):峰值 estInput -78%、累计 -68%、Tier3 只摘 delta、单调边界前缀复用。

## 其它诚实边界

- **费用差为上界**:Tier3 增量摘要器直连 LlmClient,其自身 LLM 开销不经 `onUsage`、未计入 on 侧,故 on 侧真实成本略高于此处。
- **默认阈值 128k**:多数任务未跨 60%(≈76.8k)→ curator 少触发甚至不触发,on-default 与 off 差异小属预期(curator 是长会话保险)。
- **压力阈值**(tier1=0.15)仅为可控成本下暴露力学,非默认触发点。
